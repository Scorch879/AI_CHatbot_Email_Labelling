# config.yaml
manager:
  workspace_dir: "./skills"

models:
  default: "gemini-2.5-flash"
  providers:
    google:
      api_key: "${GEMINI_API_KEY}"
      model: "gemini-2.5-flash" # Fast and accurate for data extraction